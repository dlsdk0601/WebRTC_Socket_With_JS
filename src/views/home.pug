doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body 
    header 
        h1 It works!
    main    
        div#welcome
            form
                input#roomname(placeholder="room name", requried, type="text")
                input#name(placeholder="nickname", requried, type="text")
                button Save
        div#room
            h3
            ul 
            form#msg
                input(placeholder="message", requried, type="text")
                button Send
        div#roomList
            h4 Open Rooms:
            ul
    script(src="/socket.io/socket.io.js")
    //- socket을 install 하면 기본적으로 제공해주는js 프론트단에서 설치 꼭 필요
    script(src="/public/js/app.js")